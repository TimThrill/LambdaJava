/**
 * Created by cong on 17/05/17.
 *
 * Lambda expression can be used in a functional interface
 * The term "functional interface" is an interface with only one abstract method
 * In fact, conversion to a functional interface is the only thing you can do with
 * a lambda expression in Java.
 */
public interface LambdaInterface {
    void printLambda();
}
